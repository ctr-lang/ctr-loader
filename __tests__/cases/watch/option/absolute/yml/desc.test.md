__Description__: Should be able to pass the last arg as a option Object notify the loader the path is absolute via `absolute: true` or `abs: true` cus the loader assumes a relative path


__Notes__

+ `ctr.yml` test
